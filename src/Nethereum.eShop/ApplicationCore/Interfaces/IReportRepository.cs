using Nethereum.eShop.ApplicationCore.Entities.RulesEngine;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace Nethereum.eShop.ApplicationCore.Interfaces
{
    /// <summary>
    /// 
    /// This interface will be the proxy of the persistence layer for reports generated by the rules engine.
    /// Even though it could inherit the IAsyncRepository interface, perhaps it should inherit an alternative
    /// interface that binds to a different persistence implementation, since reports should only be WORM 
    /// (written once, read many).  This makes reports unlike other objects stored in repositories.
    ///
    /// </summary>
    public interface IReportRepository
    {
        Task<List<RuleTreeReport>> GetByRuleTreeIdAsync(int ruleTreeId);
    }
}
